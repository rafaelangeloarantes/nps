---
description: Gera ou completa estrutura base Node (Express) + PostgreSQL + React (Vite)
---

# Scaffold full-stack Node + React + Postgres

Aplique a skill **node-react-postgres-scaffold** em `.cursor/skills/node-react-postgres-scaffold/SKILL.md`.

Objetivo: garantir pastas e arquivos mínimos **backend/** (Express, `pg`, `dotenv`, `cors`), **frontend/** (Vite + React com proxy para `/api`), **sql/schema.sql**, e **backend/.env.example**.

Se algo já existir, **complete** o que faltar sem reescrever o que estiver funcional. Use a stack e convenções das regras em `.cursor/rules/`.

Ao final, liste o que foi criado ou alterado e como rodar backend, frontend e aplicar o SQL.

**Consulta humana:** `.cursor/guias/inicio-projeto-node-react-postgres.md` e `.cursor/guias/checklist-inicio.md`.
