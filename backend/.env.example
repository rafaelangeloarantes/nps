PORT=3000
NODE_ENV=development

# Banco do app NPS (ajuste host/usuário/senha)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=nps
DB_USER=postgres
DB_PASSWORD=

# Alternativa: URL única (comentário — descomente e preencha se preferir)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/nps